import { StyleSheet } from "react-native";

export const BtnStyles = StyleSheet.create({
    container: {
        width: '45%',
        borderWidth: 1,
        borderRadius: 10,
        padding: 5,
        alignItems: 'center',
        justifyContent: 'center'
    },
    titulo: {
        fontSize: 25,
        fontWeight: 'bold',
        textAlign: 'center'
    }
})